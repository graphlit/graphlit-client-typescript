query GetFact($id: ID!, $correlationId: String) {
  fact(id: $id, correlationId: $correlationId) {
    id
    creationDate
    owner {
      id
    }
    text
    status
    validAt
    invalidAt
    relevance
  }
}
